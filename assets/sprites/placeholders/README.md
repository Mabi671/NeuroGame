# Placeholder Sprites

These files document the placeholder sprites currently used by the Python
isometric engine. Add final artwork here or update `asset_path` entries in
`placeholders.json` when you are ready to replace the generated placeholder
shapes.

Current spirit placeholders:

- `spirit_placeholder`
- `spirit_fire_placeholder`
- `spirit_forest_placeholder`
